# V1.9.3

> **自本版起 OS3 / OS4 共用同一个 GitHub Release**（tag `v1.9.3`），附件含两个 APK。  
> `versionName` 仍区分渠道：`1.9.3(OS4)` / `1.9.3(OS3)`，请按系统安装对应包，勿交叉安装。

## 下载

| 渠道 | 适用系统 | APK |
|------|----------|-----|
| **OS4** | HyperOS 4 / Android 17 | `LyricFocus.v1.9.3.OS4.apk`（包内 versionName `1.9.3(OS4)`） |
| **OS3** | HyperOS 3.x | `LyricFocus.v1.9.3.OS3.apk`（包内 versionName `1.9.3(OS3)`） |

## 新增（两渠道）
- **按歌曲 ID 直取歌词**：网易云 / QQ 音乐 / 小米音乐优先用平台 ID 拉歌词，失败再回退标题+歌手搜索
- **多行 · 仅当前行显示翻译**：开启后仅当前句下方显示翻译，后续行只显示原文
- **多行行距可调**：设置中调节原文行间距（0–100dp，默认 50）；翻译紧跟间距仍为 2dp

## 优化
- 关于页：仓库/网盘链接整组居中；项目描述按渠道标明 OS3/OS4，并说明支持向第三方软件推送与转发歌词
- 修复更新检测：`1.9.3(OSx)` 与 tag `v1.9.3` 比较时忽略渠道后缀，避免已是最新仍提示更新；下载优先匹配本渠道 APK

## 版本号
- OS4：`1.9.3(OS4)`（versionCode 34）
- OS3：`1.9.3(OS3)`（versionCode 36）
