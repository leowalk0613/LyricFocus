LyricFocus｜一款小米HyperOS 3.0专用的AOD歌词焦点通知软件（基于V1.8版本）

LyricFocus 是一款小米 HyperOS 3.0 专用的 AOD 歌词焦点通知模块。基于 LSPosed 框架，通过 Hook 系统界面（SystemUI）和息屏显示（AOD），利用小米焦点通知（miui.focus.*）在锁屏和息屏上实时展示同步歌词与翻译。

从 v1.0 到 v1.8.0，本项目已历经数十个版本的迭代。v1.8.0 更是完成了底层架构的重大升级——完整迁移至 Modern Xposed API（LSPosed 2.0 / API 102），带来了诸多新功能和体验优化。

项目仓库：https://github.com/leowalk0613/LyricFocus
网盘地址：https://1825191091.mshare.123pan.cn/123pan/jNBsjv-vZrV?pwd=Ifn3
Gitee 地址：https://gitee.com/leowalk0613/LyricFocus


⚠️ 安装前必读

虽然本应用没有直接修改系统文件，但因为它 Hook 了系统界面（com.android.systemui）和息屏模块（com.miui.aod），不排除在极少数情况下可能导致 SystemUI 崩溃，进而影响正常使用。搞机有风险，安装需谨慎。

本人不对因安装本模块导致的系统损坏或数据丢失负责。请务必在充分阅读本图文和项目仓库 Readme，做好必要的数据备份和救砖准备后，再进行安装。

点击展开，查看详细安装要求与使用教程

📱 安装要求

1. 小米 HyperOS 3.0+（验证环境：HyperOS 3.0.302.0.WNCCNXM）
2. ⚠️ LSPosed 2.0（API 102）—— v1.8.0 已升级至 Modern Xposed API，旧版 LSPosed 无法使用
3. 解锁 BootLoader 并获取 Root 权限
4. 在 HyperIsland、HyperCeiler 等模块中开启"移除焦点通知白名单"功能（否则 LyricFocus 无法正常推送焦点通知）

📖 使用教程

1. 下载 LyricFocus.v1.8.0.apk 安装到手机（若曾安装旧版本请先卸载）
2. 打开应用，按提示授予「通知访问」「发送通知」两个必要权限
3. 在 Root 管理器中授予 LyricFocus Root 权限（可选，用于应用内重启 SystemUI）
4. 在 LSPosed 管理中启用 LyricFocus，勾选作用域：系统界面（com.android.systemui）和息屏与锁屏编辑（com.miui.aod）
5. 重启 SystemUI 或重启手机，确保服务状态显示"运行中"
6. 播放音乐，享受锁屏/AOD 歌词体验

为了后台稳定性，建议额外操作：
- 设置 → 应用 → LyricFocus → 省电策略设为「无限制」
- 开启自启动权限

点击收起


一、核心功能介绍

1. 焦点通知歌词总开关

应用主界面顶部提供「焦点通知歌词」总开关，默认开启。关闭后所有歌词相关的焦点通知功能停止运作，相当于一键暂停模块功能。

（📷 留空：应用主界面截图）


2. 万象息屏（自定义）AOD

万象息屏模式下，提供独立的横向 AOD 布局方案。开启后锁屏样式的 AOD 设置项自动置灰，万象息屏专属样式设置激活。

v1.8.0 新增：万象息屏模式开启后，播放音乐时自动隐藏其他焦点通知和普通通知，AOD 上仅展示歌词通知。亮屏或暂停播放后所有通知恢复正常。

（📷 留空：万象息屏开关截图）


3. 音乐应用白名单

可限制仅对指定音乐 App 的播放推送歌词焦点通知。支持从已安装应用列表中选择，也可手动填写包名添加。推荐开启以避免非音乐类通知干扰。

（📷 留空：白名单管理界面截图）


4. 歌词获取源

提供多种歌词获取方式，按优先级自动切换或手动指定：

- 自动模式：依次尝试网易云音乐 → QQ 音乐，找到即停
- 网易云音乐：直接调用网易云 API 搜索歌词
- QQ 音乐：通过 QQ 音乐 API 获取歌词
- 本地 LRC 文件：读取手机本地存储的 .lrc 歌词文件，支持自选文件夹
- AI 翻译：基于 OpenAI Chat Completions 格式 API，在线翻译歌词（可配置 Base URL、API Key、模型名称、目标语言）

歌词获取源旁显示当前命中的服务来源及歌曲信息，便于排查问题。

（📷 留空：歌词源设置界面截图）


5. 歌词同步偏移

可调节歌词切换时机，范围 -1000ms 至 +3000ms，默认提前 200ms。歌词偏慢向右调，偏快向左调。

（📷 留空：同步偏移滑块截图）


6. 服务状态

实时显示服务运行状态（运行中 / 未运行），绿色表示正常。若服务未启动，可尝试重新授予通知权限。


二、权限设置

应用主界面下方提供多项权限状态的实时检测与一键跳转设置：

- 通知访问权限（必要）：监听音乐媒体会话，获取当前播放信息与歌词同步进度
- 发送通知权限（必要）：维持前台服务保活，发送焦点歌词通知
- Root 权限（可选）：应用内一键重启 SystemUI，查看 LSPosed 日志
- 读取应用列表（可选）：在白名单中选择已安装的音乐 App
- 自启动权限（可选）：开机或解锁后自动拉起歌词服务
- 忽略电池优化（可选）：减少后台被系统冻结，保证歌词同步稳定性

每项权限均有状态图标（绿勾 / 红叉）和用途说明，方便逐项检查。

（📷 留空：权限设置卡片截图）


三、样式设置

样式设置页面分为三大板块：通用样式、锁屏样式 AOD、万象息屏 AOD。

3.1 通用样式（全部样式生效）

- 歌词与翻译位置互换：当歌词有翻译时，第一行显示翻译、第二行显示原文
- 仅显示第一行：隐藏第二行（翻译或歌词），只保留第一行
- Monet 动态取色：按 Material You 风格从专辑封面实时提取背景色与文字色，自动增强对比度保证可读性。开启后手动背景/文字颜色选项置灰
- 通知文字取色：从专辑封面提取主色调，仅影响文字颜色，背景保持手动设置
- 焦点通知背景：可选默认（透明）、纯黑、纯白

（📷 留空：通用样式设置截图）


3.2 锁屏样式 AOD

- 文字大小：歌词字号 12-32sp 可调
- 文字颜色：白色 / 黑色
- 固定行数：歌词第一行 1-2 行，第二行（翻译/次行）1-2 行
- 排版：靠左 / 居中 / 靠右
- 多行模式（v1.6.2）：支持 4 / 6 / 8 行按页显示。有翻译时可交错显示原文与更淡翻译（可开关）；当前播放行字号增大 20% 并使用专辑主打色高亮

开启万象息屏后，此板块整体置灰不可调整。

（📷 留空：锁屏样式设置截图）


3.3 万象息屏 AOD 专属样式

开启主界面的「万象息屏（自定义）AOD」开关后激活：

- 文字大小：独立字号 12-32sp
- 歌词宽度：占屏比例 50%-100%
- 歌名显示：全部显示 / 隐藏歌名 / 隐藏歌手 / 全部隐藏
- 标题图标：歌名前显示音乐应用图标（自动识别网易云/QQ/酷狗/酷我/汽水/Spotify/Apple Music），可调节图标大小
- 固定行数：歌词/翻译各 1-2 行
- 排版：靠左 / 居中 / 靠右
- 歌词颜色：白色 / 专辑主色取色 / 24 色推荐色 + RGB 自定义颜色

（📷 留空：万象息屏样式设置截图）


四、AOD 实时刷新

v1.7.0 重构后，锁屏和 AOD 的歌词换行统一使用 updatable notify-only 模式（通知内容原地更新，不重建通知会话），不再出现焦点通知闪烁/入场动画。

v1.8.0 进一步优化：万象息屏和锁屏 AOD 使用同一刷新规则。Hook AodFocusControllerV2 为歌词通知强制设置 enableAlert，确保息屏时 AOD 进程能收到更新；同时 Hook AOD 进程的 MiuiFocusNotification2，在锁屏↔AOD 状态切换时保护歌词视图不被清除。


五、多行歌词模式

v1.6.2 引入的多行模式在锁屏样式 AOD 下可用，选择 4 / 6 / 8 行按页面刷新锁屏焦点通知：

- 无翻译时：当前段落原文填满所选行数
- 有翻译时：原文与翻译交错排列，翻译行颜色更淡以区分
- 当前播放行高亮：字号增大 20%，颜色使用专辑主打色
- 可独立调整多行字号 12-32sp

（📷 留空：多行歌词效果截图）


六、万象息屏仅显示歌词（v1.8.0 新增）

v1.8.0 新增功能：开启万象息屏模式后，播放音乐时 AOD 自动隐藏除歌词以外的所有其他关注通知。歌词通知独占 AOD 显示空间，简洁纯粹。

此功能仅在以下条件同时满足时生效：万象息屏模式开启 + 正在播放音乐 + 屏幕息屏（AOD 活跃状态）。亮屏或暂停播放后，所有隐藏的通知自动恢复正常显示。

技术实现：Hook KeyguardNotificationVisibilityProviderImpl.shouldHideNotification 和 AodFocusControllerV2.onAdd，在数据层过滤非歌词的通知条目。


七、歌词置顶

在焦点通知列表中，歌词通知始终保持在第一位（高于倒计时、HyperIsland 等其他焦点通知）。v1.8.0 增强了置顶机制，覆盖 FocusedNotifPromptView 数据加载和 ShadeListBuilder 通知变更入口，即使倒计时推送触发焦点通知也不会把歌词挤到第二位。


八、应用界面优化

v1.5 至 v1.8 持续优化应用 UI：

- 统一的 Material 3 Tonal 风格界面
- 主页、样式设置、关于页三个底部导航 Tab
- 工具栏常驻版本信息按钮（有新版本时按钮变红，无新版本时白色可查看当前版本日志）
- 主页右上角一键重启 SystemUI 图标（需 Root）
- 版本检测支持并行请求 GitHub / Gitee Release，后台线程不阻塞界面
- v1.8.0 修复主页和样式设置页面底部按钮需滑至最底端的问题
- v1.8.0 修复版本弹窗首次点击自动关闭

（📷 留空：应用主界面完整截图）


九、关于页面

提供以下功能与信息：

- 应用基本信息与版本号
- 系统要求弹窗（点击卡片右上角 ℹ️ 图标查看）
- 项目信息与联系作者（邮箱：walkalone9990613@gmail.com）
- 日志获取功能：自动扫描 /data/adb/lspd/log/ 目录，或手动选择日志文件/ZIP 压缩包，自动筛选 LyricFocus 相关日志，支持一键复制
- 开源许可与致谢

（📷 留空：关于页面完整截图）


十、常见问题与反馈

Q. 安装了模块但锁屏/AOD 没有歌词？
A. 请依次检查：
1. LSPosed 是否已启用模块并勾选了 com.android.systemui 和 com.miui.aod 两个作用域
2. 是否已授予通知访问和发送通知两项必要权限
3. 是否已在 HyperIsland / HyperCeiler 等模块中开启了"移除焦点通知白名单"
4. 服务状态是否为"运行中"
5. 以上确认无误后，重启 SystemUI（应用内右上角按钮或 Root 终端输入 pkill -f systemui）

Q. v1.8.0 要求 LSPosed 2.0（API 102），我的 LSPosed 能用吗？
A. 打开 LSPosed 管理 → 设置 → 查看版本号。如果版本号低于 2.0，请先升级 LSPosed。不升级的话 v1.8.0 无法正常加载。

Q. 闪退 / SystemUI 崩溃？
A. 重启手机进入安全模式，卸载本应用即可恢复。请带日志反馈给作者。

Q. 有通知但没有歌词文本 / 只显示占位符？
A. 当前歌曲可能在网易云 / QQ 音乐 API 上搜索不到。尝试切换歌词源，或检查网络与 DNS（部分区域需稳定网络访问 music.163.com / c.y.qq.com）。

Q. 如何获取日志反馈问题？
A. 路径 /data/adb/lspd/log/，找到 modules_（当前日期时间）.log 文件。复制到 App 可访问的目录后，进入 LyricFocus 关于页 → 选择日志文件筛选 → 一键复制。将日志内容发送给作者。


最后

LyricFocus 从 v1.0 开始依赖 AI 辅助编程完成大部分开发工作（感谢 Trae CN、Cursor 等工具的帮助），也参考了大量开源项目的代码实现，在此特别感谢所有开源作者的付出。

目前项目仍在持续迭代中，v1.8.0 是一次重大的架构升级，标志着从 legacy Xposed API 到 Modern Xposed API 的完整过渡。后续版本计划包括更多样式自定义选项、更稳定的置顶机制等。

欢迎各位朋友下载体验，也欢迎大佬们对项目提出宝贵的意见与建议。本项目开源，协议 MIT。如有问题请通过项目 Issues 或酷安私信反馈 ❤️。

#HyperOS3# #LSPosed# #焦点通知# #AOD歌词#
