<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="false"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.ExternalLyricTest">

        <!-- LyricFocus 扫描此 meta-data 发现外部渲染 endpoint -->
        <meta-data
            android:name="com.leowalk.LyricFocus.EXTERNAL_LYRIC"
            android:value="content://com.leowalk.ExternalLyricTest.lyric" />

        <provider
            android:name=".LyricReceiverProvider"
            android:authorities="com.leowalk.ExternalLyricTest.lyric"
            android:exported="true"
            android:grantUriPermissions="false" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
